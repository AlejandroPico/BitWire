[package]
name = "bitwire-core"
version = "0.1.0"
edition = "2021"
description = "Deterministic hierarchical circuit core for BitWire"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wasm-bindgen = "0.2"

